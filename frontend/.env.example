# Base URL of the Salon backend. Do not include /api or a trailing slash.
VITE_API_URL=http://localhost:5000

# The deployed frontend origin must be included in backend CLIENT_URLS.
# Vite will fail instead of silently switching development ports.
VITE_DEV_PORT=5173
